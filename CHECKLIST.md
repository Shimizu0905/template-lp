# 本番環境デプロイ前チェックリスト

## ✅ 確認済み項目

### 1. ビルドファイルの存在
- ✅ `assets/css/min-style.css` (43KB) - 存在確認済み
- ✅ `assets/js/min-script.js` (3.2KB) - 存在確認済み
- ✅ `assets/css/script.css` (57KB) - Viteが生成
- ✅ `assets/js/script.js` (4.8KB) - Viteが生成
- ✅ `assets/.vite/manifest.json` - 存在確認済み

### 2. 新しいセクションのスタイル確認
- ✅ `p-fv` - min-style.cssに含まれている
- ✅ `p-number` - min-style.cssに含まれている
- ✅ `p-reason` - min-style.cssに含まれている
- ✅ `p-interview` - min-style.cssに含まれている
- ✅ `p-recruit` - min-style.cssに含まれている
- ✅ `p-flow` - min-style.cssに含まれている
- ✅ `p-contact` - min-style.cssに含まれている

## ⚠️ 修正が必要な項目

### 1. `functions.php`のmanifest.jsonパス
**問題**: `functions.php`が`assets/manifest.json`を探していますが、実際のファイルは`assets/.vite/manifest.json`にあります。

**修正方法**:
```php
// 47行目を修正
$manifest_path = $theme_dir . '/assets/.vite/manifest.json';
```

### 2. 圧縮版ファイルの使用
**現在の実装**: manifest.jsonから`script.css`と`script.js`を読み込んでいます（圧縮されていない）

**推奨**: 本番環境では圧縮版（`min-style.css`と`min-script.js`）を使用する方が良いです。

**選択肢**:
- **オプションA**: manifest.jsonベースのまま（`script.css`と`script.js`を使用）
- **オプションB**: 圧縮版を直接読み込む（`min-style.css`と`min-script.js`を使用）← 推奨

## 📋 本番環境での設定

### wp-config.php
```php
// 本番環境では以下を設定
define('WP_ENVIRONMENT_TYPE', 'production');
define('WP_DEBUG', false);
define('WP_DEBUG_LOG', false);
define('WP_DEBUG_DISPLAY', false);
define('SCRIPT_DEBUG', false);
```

## 🔍 デプロイ後の確認事項

1. **ブラウザの開発者ツールで確認**
   - Networkタブで、正しいCSS/JSファイルが読み込まれているか
   - `min-style.css`または`script.css`が読み込まれているか
   - `min-script.js`または`script.js`が読み込まれているか
   - `localhost:3000`へのリクエストがないこと（ViteのHMRが動作していないこと）

2. **スタイルの確認**
   - すべてのセクションが正しく表示されているか
   - レスポンシブデザインが正しく動作しているか

3. **JavaScriptの動作確認**
   - タブ切り替え（募集要項）
   - FAQアコーディオン
   - ハンバーガーメニュー
   - その他のインタラクティブ要素

## 📝 アップロードするファイル

### 必須ファイル
- `functions.php`（修正後）
- `front-page.php`
- `header.php`
- `footer.php`
- その他のテンプレートファイル

### アセットディレクトリ
- `assets/`ディレクトリ全体
  - `assets/css/`（`min-style.css`または`script.css`）
  - `assets/js/`（`min-script.js`または`script.js`）
  - `assets/.vite/manifest.json`（manifest.jsonベースの場合）
  - `assets/images/`（画像ファイル）

### 不要なファイル（アップロード不要）
- `src/`ディレクトリ（開発用ソースコード）
- `.config/`、`scripts/`、`node_modules/`（開発用）
- `.vite/`ディレクトリ（manifest.json以外）
